services:
  - type: web
    name: autonomous-ai-api
    env: python
    pythonVersion: 3.11
    buildCommand: pip install --no-deps --no-build-isolation -r requirements.txt && pip install starlette click h11
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
