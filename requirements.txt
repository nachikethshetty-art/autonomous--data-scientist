# Absolute minimal - SKIP pandas to avoid Python 3.14 compilation issues
# Lazy load pandas only when DataValidator is actually used
fastapi==0.95.2
uvicorn==0.21.0
requests==2.31.0
numpy==2.1.0
python-dotenv==1.0.0
